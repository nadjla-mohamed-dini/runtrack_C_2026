#include <stdlib.h>

int is_space(char c) {
    return (c == ' ' || c == '\t' || c == '\n');
}

    int i = 0;
int count_words(const char *str)
{
    int i = 0;
    int count = 0;

    while (str [i] != '\0'){
        count ++;
        while (str[i]);
    }
}